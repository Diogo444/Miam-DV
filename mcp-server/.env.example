# --- Backend API target ---
# Local dev: http://localhost:3000
# Docker compose: http://backend:3000
API_BASE_URL=http://localhost:3000

# --- Auth towards backend (choose one) ---
MCP_API_KEY=change_me
# MCP_SERVICE_JWT=change_me

# --- MCP HTTP server ---
# Use 127.0.0.1 for local-only, or 0.0.0.0 to expose on the network
MCP_HTTP_HOST=127.0.0.1
MCP_HTTP_PORT=4310

# --- Optional: protect MCP server itself ---
MCP_SERVER_AUTH_TOKEN=change_me

# --- Optional: allowlists ---
MCP_ALLOWED_HOSTS=localhost,127.0.0.1
MCP_ALLOWED_ORIGINS=http://localhost
