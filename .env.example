# --- MySQL ---
MYSQL_ROOT_PASSWORD=change_me
MYSQL_DATABASE=miamdv
MYSQL_USER=miammi
MYSQL_PASSWORD=change_me

# --- Backend (NestJS) ---
JWT_SECRET=change_me
JWT_EXPIRES_IN=1d
# Comma-separated allowed origins (ex: https://app.tld,https://admin.tld)
CORS_ORIGIN=https://your-domain.tld
DB_SYNC=false

# --- Reverse-proxy network (external) ---
# This network must exist before `docker compose up`:
# docker network create reverse-proxy
PROXY_NETWORK=reverse-proxy

# --- MCP (NestJS guard + MCP server) ---
MCP_API_KEY=change_me
MCP_API_KEY_SCOPES=mcp:menu:write,mcp:proverb:write,mcp:week:clear
MCP_RATE_LIMIT_WINDOW_MS=60000
MCP_RATE_LIMIT_MAX=60

# --- MCP server (HTTP) ---
MCP_HTTP_PORT=4310
MCP_SERVER_AUTH_TOKEN=change_me
MCP_ALLOWED_HOSTS=localhost,127.0.0.1
MCP_ALLOWED_ORIGINS=https://your-domain.tld
